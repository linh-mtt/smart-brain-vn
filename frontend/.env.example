# ─── SmartMath Kids — Frontend Environment ───────────────
# This file serves as a template for frontend configuration.
#
# Multi-env loading order (highest priority first):
#   .env.[APP_ENV].local  →  .env.[APP_ENV]  →  .env.local  →  .env
#
# APP_ENV values: development | staging | production
# Set APP_ENV in the backend or system environment.
# ─────────────────────────────────────────────────────────
API_BASE_URL=http://localhost:3000
WS_URL=ws://localhost:3000/api/v1/ws
