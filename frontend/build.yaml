targets:
  $default:
    builders:
      freezed:
        options:
          union_key: type
          union_value_case: pascal
      json_serializable:
        options:
          explicit_to_json: true
          checked: true
